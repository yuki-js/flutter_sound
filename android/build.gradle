
group 'xyz.canardoux.fluttersound'
version '9.29.14'

buildscript {
    repositories {
        google()
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:8.6.0'
    }
}

rootProject.allprojects {
    repositories {
        maven { url 'https://jitpack.io' }
        google()
        mavenCentral()
    }
}

apply plugin: 'com.android.library'

android {
// Conditional for compatibility with AGP <4.2.
    if (project.android.hasProperty("namespace")) {
      namespace 'xyz.canardoux.fluttersound'
    }    
    
    compileSdkVersion 35
    buildToolsVersion "35.0.0"

    defaultConfig {
        minSdkVersion 24 // 18 works fine for the player, but the recorder needs at least 24
        //targetSdkVersion 34
    }
    lintOptions {
        disable 'InvalidPackage'
    }
//    compileOptions {
//        sourceCompatibility JavaVersion.VERSION_22
//        targetCompatibility JavaVersion.VERSION_22
//    }
}


String safeExtGet(String prop, String fallback) {
    rootProject.ext.has(prop) ? rootProject.ext.get(prop) : fallback
}


dependencies {
    implementation 'androidx.core:core:1.3.2'
    implementation 'androidx.media:media:1.4.1'
    implementation 'androidx.appcompat:appcompat:1.2.0'

    // -------------------------------------------------------------------------------------
    // The TauEngine classes are now included locally in this package with the race condition fix
    // for feedFromStream(). The external flutter_sound_core dependency is no longer needed.
    // See: https://github.com/Canardoux/flutter_sound/issues/1123
    // -------------------------------------------------------------------------------------

}

